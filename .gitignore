# Claude Code memory
.claude/

# Superpowers brainstorming visual companion (local scratch mockups)
.superpowers/

# VM network login credentials (never commit)
docs/network-login.config.json

# VM startup run log
docs/startup.log

# VM network keep-alive run log
docs/network-keepalive.log

# Test reports (generated by Playwright)
*.pdf
testcases_*.txt

# DB dumps (may contain real student PII - never commit)
*.sql
!backend/prisma/migrations/**/*.sql

# OS files
.DS_Store
Thumbs.db
desktop.ini
